<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="50dp" />

    <!-- Gradient stroke with Google colors -->
    <stroke
        android:width="1dp"
        android:color="@color/google_blue" /> <!-- Change to the desired Google color -->
</shape>
